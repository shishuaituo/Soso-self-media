---
name: "Soso-self-media"
description: "家居建材行业自媒体运营助手，聚焦全屋定制、板材赛道。基于 Firefly 社媒数据 API，支持抖音和小红书的选题挖掘、对标账号分析、爆款结构拆解和内容初稿生成。当用户需要找家居建材选题、分析定制/板材账号、拆解爆款内容或生成内容初稿时调用。"
---

# Soso-self-media 家居建材自媒体实验室

**聚焦赛道：家居建材 → 全屋定制 → 板材**

单人自媒体降维打法——用 AI 搞定从选题到初稿的整套运营工作流，专为家居建材、全屋定制、板材行业打造。

---

## 行业知识库（内置）

### 一、赛道关键词库

#### 板材类关键词
- **板材种类**：颗粒板、多层板、生态板、密度板、欧松板（OSB）、实木板、指接板、禾香板、颗粒板、中纤板、刨花板、胶合板、细木工板
- **环保等级**：ENF 级、E0 级、E1 级、无醛添加、MDI 胶、F4 星
- **板材品牌**：兔宝宝、莫干山、千年舟、露水河、爱格板（EGGER）、克诺斯邦、菲德耐、大亚、圣象、万华
- **饰面工艺**：三聚氰胺饰面、PET 饰面、烤漆、免漆、包覆、吸塑、UV 漆
- **板材特性**：防潮、防火、耐磨、抗变形、握钉力、环保、性价比

#### 全屋定制类关键词
- **空间分类**：衣柜定制、橱柜定制、榻榻米、书柜、酒柜、鞋柜、电视柜、衣帽间、阳台柜、餐边柜
- **风格分类**：现代简约、北欧风、轻奢风、新中式、美式、欧式、工业风、日式、奶油风、侘寂风
- **材质组合**：板式定制、实木定制、钢木结合、玻璃门、岩板
- **五金配件**：铰链、滑轨、拉手、拉直器、反弹器、挂衣杆、抽屉轨道、骑马抽
- **五金品牌**：百隆（Blum）、海蒂诗（Hettich）、DTC、悍高、诺米
- **计价方式**：投影面积、展开面积、套餐价、延米

#### 装修/家居类关键词
- **流程节点**：装修避坑、装修日记、装修攻略、装修预算、装修干货、装修经验
- **空间设计**：小户型设计、收纳设计、动线设计、灯光设计、色彩搭配
- **人群场景**：刚需装修、改善型住房、婚房装修、旧房改造、出租屋改造
- **品牌/品类**：定制衣柜品牌、全屋定制品牌、板材品牌推荐

#### 痛点类关键词（高转化）
- 避坑类：全屋定制避坑、板材避坑、装修踩坑、不建议做、后悔装了
- 对比类：颗粒板 vs 多层板、E0 vs ENF、定制 vs 木工打、品牌 vs 本地工厂
- 预算类：全屋定制多少钱、性价比高的板材、省钱攻略
- 科普类：板材怎么选、环保等级怎么看、五金怎么选
- 揭秘类：全屋定制套路、板材行业内幕、销售不会告诉你的

---

### 二、目标受众画像

| 受众类型 | 特征 | 核心需求 | 关注平台 |
|----------|------|----------|---------|
| 装修业主 | 25-40岁，准备装修或正在装修 | 避坑、省钱、效果好 | 小红书 > 抖音 |
| 定制门店销售 | 建材/定制行业从业者 | 获客、谈单技巧、产品知识 | 抖音 > 小红书 |
| 设计师 | 室内设计师、全屋定制设计师 | 灵感、材料、工艺 | 小红书 > 抖音 |
| 木工/工长 | 装修施工人员 | 材料知识、施工技巧 | 抖音 |
| 建材经销商 | 板材、五金经销商 | 行业动态、选品、经营 | 抖音 |

---

### 三、内容选题方向分类（家居建材行业）

#### 1. 避坑揭秘类（高流量高互动）
- 全屋定制 XX 个避坑点
- 板材销售不会告诉你的真相
- 这 X 种板材千万别选
- 全屋定制套路大揭秘
- 装修踩过的血泪坑

#### 2. 对比测评类（高收藏高转化）
- 颗粒板 vs 多层板，到底选哪个？
- E0 级 vs ENF 级，差在哪里？
- 品牌定制 vs 本地工厂，差在哪？
- 投影面积 vs 展开面积，哪个更划算？
- 十大板材品牌对比测评

#### 3. 科普干货类（建立专业度）
- 一篇搞懂板材环保等级
- 全屋定制流程全解析
- 五金件怎么选？一篇讲透
- 不同空间的衣柜设计要点
- 板材饰面工艺大盘点

#### 4. 效果展示类（视觉冲击）
- XX 平小户型全屋定制完工实拍
- 沉浸式参观定制衣柜
- 一镜到底看全屋
- 装修前后对比
- 网红款衣柜实拍

#### 5. 预算价格类（高精准）
- 100 平全屋定制要花多少钱？
- 不同板材价格差多少？
- 省钱攻略：这样定制省 30%
- 预算有限怎么选板材？
- 全屋定制报价单详解

#### 6. 装修日记/经验类（强人设）
- 装修第 X 天，定制进场了
- 我的全屋定制全过程记录
- 自装半年，我总结了这些经验
- 第一套房踩的坑，第二套房全避开了

#### 7. 选品推荐类（带货向）
- 性价比最高的 5 款板材
- 闭眼入的五金品牌推荐
- 这几个板材品牌别再买错了
- 设计师私藏的板材好物

---

### 四、家居行业爆款结构特点

1. **开头钩子**：
   - 数字式："全屋定制 10 大坑，第 3 个 90% 的人都踩过"
   - 提问式："全屋定制到底选颗粒板还是多层板？"
   - 反差式："别再被销售忽悠了，ENF 级板材根本不是最好的"
   - 揭秘式："板材行业内幕，今天我全说出来"

2. **内容结构**：
   - 清单式：X 个要点，每个配案例
   - 对比式：A vs B，分维度对比
   - 流程式：从 X 到 Y，一步一步讲
   - 避坑式：X 个坑，每个说清楚为什么坑、怎么避

3. **结尾引导**：
   - 互动式："你们家选的什么板材？评论区聊聊"
   - 收藏式："收藏起来，装修的时候照着选"
   - 关注式："关注我，全屋定制不踩坑"
   - 引流式："需要板材报价表的，评论区打'想要'"

### 雪宝品牌专属知识库（内置）

> 如果用户配置的品牌是「雪宝板材」，自动加载以下品牌信息，在生成内容时自然植入。

#### 品牌核心定位
- **定位**：儿童健康板材首创品牌 → 儿童健康板材全国销量第一
- **slogan**：儿童房健康板 / 儿童安全级 / 让万千家庭放心
- **创立**：2009年品牌诞生，核心团队1996年进入行业（30年+深耕）
- **规模**：2000+销售网点，覆盖600+城市，累计服务1亿业主
- **荣誉**：140+项权威荣誉（2016-2026累计）

#### 核心数据（文案可用的强说服力数字）

| 数据 | 含义 | 用法 |
|------|------|------|
| **优于ENF级20%** | 甲醛释放≤0.02mg/m³（国标ENF≤0.025） | 环保类内容的核心卖点 |
| **99.7%优品率** | 高于行业平均97% | 品质信任背书 |
| **0.1%变形率** | 仅为国标1%的1/10 | 稳定性卖点 |
| **46道工序** | 41项严选标准，45天养生 | 匠心/品质内容 |
| **80+项专利** | 含国家发明专利大豆胶 | 技术实力 |
| **儿童健康板材全国销量第一** | 尚普咨询认证 | 市场地位声明 |
| **抖音家居甄选榜全国TOP1** | 2026年 | 数字化/影响力 |
| **30年+** | 核心团队深耕行业 | 品牌资历 |

#### 核心产品矩阵

**板材类（8大系列）：**

| 产品 | 核心卖点 | 一句话话术 |
|------|----------|-----------|
| ENF实木生态板 | 25年香杉老树、7层结构、榫卯齿接、免漆饰面 | "用的是25年的香杉老树，密度比普通树高20%" |
| ENF实木多层板 | 11层桉木、48.6MPa超国标51.9%、45天养生 | "静曲强度超国标一半多，承重杠杠的" |
| 净味·纯芯板（新品⭐） | 抗病毒99.9%、抗菌99.99%、0级防霉、净醛80%+、净味 | "五效合一，能抗病毒的板材，给孩子用更放心" |
| OSB智芯板 | MDI无醛胶、定向铺装、连续压平、超强握钉 | "MDI胶做的，无醛添加，承重超300斤" |
| LSB轻质可饰面板 | 5层定向、可直接饰面、表面平整 | OSB升级版，表面更细 |
| FOSB精细表面板 | 高端定制、PET饰面、超平整 | 高端定制专用 |
| SPC香杉抗变形PET门板 | 防水防火、0醛添加、一门到顶不变形 | "完全不吸水，浴室柜都能用，一门到顶不变形" |
| 纸面石膏板 | 无磷无苯、防火防潮、轻质隔音 | 绿色建材 |

**五金类（核心产品）：**

| 产品 | 核心卖点 | 一句话话术 |
|------|----------|-----------|
| 890超级静音铰链 | 5项专利、小角度缓冲、100天泡水抗锈、5维精调 | "5项专利的铰链，泡水100天都不生锈，小角度就有缓冲" |
| 312静音阻尼铰链 | 液压阻尼、7片助力臂、三层电镀 | "性价比款，液压缓冲，关门静音防撞" |
| 4515三节缓冲滑轨 | 上海宝钢、承重30kg、双弹簧消音 | "上海宝钢的料，承重30公斤，抽拉静音" |
| 隐藏式托底轨 | 隐藏安装、承重30kg、按弹开启 | "正面侧面都看不见，高级感拉满，按一下就弹开" |

**工艺类：**
- **DRIFPH德瑞夫饰面工艺**：6大维度（耐用性/还原度/吻合度/平整度/逼真度/稳定性），100%还原实木纹理
- **圆弧工艺**：高频热弯圆弧、门墙柜圆弧系统（三板块系统）
- **3重安全结构**：圆弧安全设计+开合防挤压+突出物保护

#### 核心技术壁垒

1. **微晶纤维素大豆胶**（国家发明专利）：食品级有机物配方，WHO/FDA认定安全，无醛添加，解决无毒胶耐水性差的百年难题
2. **高分子碳基生物胶**：液态游离甲醛0.02%（国标的1/15），固态下甲醛/苯/TVOC均为0检出
3. **DRIFPH德瑞夫工艺**：行业首创，夏王饰面底纸+德国HUECK钢板+瑞士艾克洛油墨，10年色泽如新
4. **全产业链闭环**：胶-板-柜-渠，从胶黏剂自研到渠道销售全打通

#### 权威认证与荣誉（精选可用）

| 荣誉 | 含金量 | 用法 |
|------|--------|------|
| 儿童健康板材全国销量第一 | 尚普咨询认证 | 市场地位，最有说服力 |
| 中国板材国家品牌 | 中国木材与木制品流通协会 | 国家级品牌背书 |
| 中国板材强国品牌 | 国字号 | 行业地位 |
| 无醛人造板分级认证 | 北京国建联信 | 环保认证 |
| 儿童安全级产品认证 | GB/T 39600-2021 | 儿童房/母婴场景 |
| 中国环境标志（十环认证） | 中环联合 | 环保背书 |
| ISO9001质量管理体系 | 方圆标志 | 品质管理 |
| 森林认证（COC/PEFC） | 中国森林认证体系 | 可持续发展 |
| 湖南省专精特新中小企业 | 湖南省工信厅 | 创新能力 |
| 抖音家居甄选榜全国TOP1 | 2026年 | 当下热度/影响力 |
| 金定奖金奖/银奖 | 连续获奖 | 产品品质 |
| 《零醛添加人造板》团体标准主编 | 2026年 | 从产品制造者到标准制定者 |

#### 品牌故事/话术素材

- **初心故事**：从1996年入行，到2010年确立"儿童房健康板"定位，因为看到太多家庭装修后孩子出问题，决心做真正环保的板材
- **品质故事**：46道工序、41项严选标准、45天养生周期，别人省的工序雪宝一道不省
- **创新故事**：自研大豆胶，解决了无毒胶不防水的行业难题，拿到国家发明专利
- **公益故事**：累计公益投入200+万元，团团希望小屋、方舱医院援建，把"儿童健康"从产品延伸到社会责任
- **数字化故事**：AI赋能终端，单店业绩增长237.6%，抖音家居甄选榜全国TOP1

#### 文案植入原则

1. **自然不硬广**：不要通篇说雪宝好，而是在讲知识点的时候自然带出"我们雪宝用的就是XX标准"
2. **用数字说话**：不说"我们很环保"，说"我们家板材甲醛释放量≤0.02mg/m³，比ENF级还优20%"
3. **场景化植入**：讲儿童房场景时提"儿童安全级认证"，讲厨房场景时提"多层板防潮性能"
4. **对比体现优势**：讲行业乱象时，自然带出雪宝的标准（如"很多商家说E0级就是最好的，其实现在最高是ENF级，我们家还优于ENF级20%"）
5. **门店/服务背书**："全国2000多家门店，你家附近应该也有，可以去看看实物"

---

## 什么时候使用这个 Skill

当用户出现以下需求时，激活本 Skill：

- "帮我找几个全屋定制/板材选题"、"最近家居行业什么话题火"
- "帮我分析一下这个定制/板材账号"、"拆解一下这个家居博主"
- "帮我看看这个家居爆款为什么火"、"拆解一下这个装修视频的结构"
- "帮我写个板材/定制视频脚本"、"生成一篇家居内容初稿"
- 用户发送抖音/小红书家居类链接并要求分析
- 用户提到家居建材、全屋定制、板材、装修相关的自媒体运营需求

---

## 快速开始

### 首次使用配置

第一次使用时，按顺序确认以下配置（缺失则询问用户）：

| 配置项 | 说明 | 默认/示例 |
|--------|------|-----------|
| `api_key` | Firefly API Key | 需用户提供（首次使用时询问） |
| `default_platform` | 默认平台 | `xhs`（家居行业小红书流量更精准） |
| `industry` | 所属细分赛道 | 默认：全屋定制 / 板材 |
| `persona` | 账号人设 | 见下方人设模板 |
| `target_audience` | 目标受众 | 默认：装修业主（25-40岁） |
| `brand_info` | 品牌/产品信息 | 代理的板材品牌、产品线等 |

> **配置记忆**：用户提供的配置信息在当前对话中持续有效，无需重复提供。

### 推荐人设模板（板材/定制行业）

**板材经销商人设**：
> "XX 板材品牌经销商，在建材行业摸爬滚打 8 年，每天分享板材干货和行业内幕，帮你选板材不踩坑。风格：直爽实在，有啥说啥。"

**全屋定制设计师人设**：
> "全屋定制设计师，做过 500+ 套定制方案，专治各种户型疑难杂症。分享定制避坑、设计灵感和预算攻略。风格：专业耐心，细节控。"

**装修业主人设**：
> "自装 2 套房的装修老炮，踩过的坑比你刷过的视频还多。分享真实装修经验和避坑心得。风格：接地气，真实分享。"

**建材门店老板人设**：
> "开了 5 年建材店的老板娘，每天分享门店日常、产品知识和销售技巧。想了解建材行业的关注我。风格：亲和亲切，像邻居聊天。"

### Firefly API 能力清单（抖音 + 小红书）

**抖音接口：**
- `视频搜索` — `/v1/douyin/video_search`（13积分/次）
- `视频详情` — `/v1/douyin/video_detail`（10积分/次）
- `视频评论` — `/v1/douyin/video_comments`（2积分/次）
- `账号信息` — `/v1/douyin/user_info`（2积分/次）
- `账号作品` — `/v1/douyin/user_videos`（2积分/次）

**小红书接口：**
- `笔记搜索` — `/v1/xhs/note_search`（13积分/次）
- `笔记详情` — `/v1/xhs/note_detail`（10积分/次）
- `笔记评论` — `/v1/xhs/note_comments`（13积分/次）
- `账号信息` — `/v1/xhs/user_info`（13积分/次）
- `账号作品` — `/v1/xhs/user_notes`（13积分/次）

### API 调用方式

使用浏览器工具的 `browser_evaluate` 调用 Firefly REST API（同域请求无 CORS 问题）。

**调用步骤：**

1. **确保在 Firefly 域名下**：先导航到 `https://firefly.qwjxqn.xyz/docs/agent-api`（只需导航一次，后续调用复用）
2. **调用 API**：使用 `browser_evaluate` 执行 fetch 请求（同域请求无 CORS 问题）

> **幂等键**：每次调用生成唯一的 `request_id`，格式 `soso_<模块>_<timestamp>_<random>`，同一参数重试时使用相同 request_id，避免重复扣费。
>
> **页面复用**：首次调用前导航到 Firefly 域名即可，后续多次 API 调用不需要重新导航，直接用 `browser_evaluate` 即可。

> 各端点的完整 Exec 调用模板见下方「API 执行模板」章节。

### API 响应数据结构（已验证）

**抖音视频搜索响应**：
```
data.business_data  → 对象，key 为 "0","1","2"...
  每个item.data.aweme_info:
    - desc           → 视频标题/描述
    - aweme_id       → 视频ID
    - author.nickname → 作者昵称
    - author.uid      → 作者UID
    - statistics.digg_count    → 点赞数
    - statistics.comment_count → 评论数
    - statistics.share_count   → 分享数
    - create_time    → 创建时间戳（秒）
```

**小红书笔记搜索响应**：
```
data.items  → 数组
  每个item.model_type === "note" 时有效
  item.note:
    - id             → 笔记ID
    - title          → 笔记标题
    - desc           → 笔记描述
    - type           → "normal"(图文) / "video"(视频)
    - liked_count    → 点赞数
    - collected_count → 收藏数
    - comments_count  → 评论数
    - shared_count   → 分享数
    - user.nickname  → 作者昵称
    - timestamp      → 创建时间戳（秒）
```

**提取代码模板**：
```javascript
// 抖音搜索结果提取
const bd = d.data?.business_data || {};
const videos = Object.values(bd)
  .map(item => item?.data?.aweme_info)
  .filter(info => info)
  .map(info => ({
    title: info.desc,
    aweme_id: info.aweme_id,
    author: info.author?.nickname,
    digg: info.statistics?.digg_count,
    comment: info.statistics?.comment_count,
    share: info.statistics?.share_count,
    create_time: new Date(info.create_time * 1000).toLocaleDateString()
  }));

// 小红书搜索结果提取
const items = d.data?.items || [];
const notes = items
  .filter(item => item.model_type === 'note' && item.note)
  .map(item => {
    const n = item.note;
    return {
      id: n.id,
      title: n.title,
      type: n.type,
      liked_count: n.liked_count,
      collected_count: n.collected_count,
      comments_count: n.comments_count,
      shared_count: n.shared_count,
      user: n.user?.nickname,
      timestamp: n.timestamp ? new Date(n.timestamp * 1000).toLocaleDateString() : null
    };
  });
```

### 平台参数差异

| 参数 | 抖音 | 小红书 |
|------|------|--------|
| 搜索方法 | POST | GET |
| 排序 | `sort_type`: "0"综合/"1"最多点赞/"2"最新 | `sort_type`: "默认"/"最多点赞"/"最新发布" |
| 时间筛选 | `publish_time`: "0"不限/"7"一周/"30"一月 | `time_filter`: "不限"/"一天内"/"一周内"/"一月内"/"半年内" |
| 分页 | `count`+`offset` | `page`+`page_size` |
| 内容类型 | 无 | `note_type`: "0"全部/"1"视频/"2"图文 |
| ID参数 | `sec_user_id` 或 `share_url` | `user_id` 或 `share_text` |
| 视频ID | `aweme_id` 或 `share_url` | `note_id` 或 `share_text` |

### API 执行模板（通过 integrated_code_mode Exec 调用）

> **重要**：以下模板中的 `USER_API_KEY` 需替换为用户提供的 Firefly API Key。
> 调用前确保浏览器已导航到 `https://firefly.qwjxqn.xyz/docs/agent-api`（整个会话只需导航一次）。

#### 0. 初始化导航（每次会话开始时执行一次）

```javascript
await tools.browser_navigate({ url: 'https://firefly.qwjxqn.xyz/docs/agent-api' });
await tools.browser_wait_for({ time: 2 });
text('Firefly 域名导航完成，可以开始 API 调用');
```

#### 1. 抖音视频搜索（13积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/douyin/video_search', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_dy_search_REQID'
      },
      body: JSON.stringify({
        keyword: 'KEYWORD',
        sort_type: '1',
        publish_time: '0',
        count: '20'
      })
    })
    .then(r => r.json())
    .then(d => {
      const bd = d.data?.business_data || {};
      const videos = Object.values(bd)
        .map(item => item?.data?.aweme_info)
        .filter(info => info)
        .map(info => ({
          title: info.desc,
          aweme_id: info.aweme_id,
          share_url: 'https://www.douyin.com/video/' + info.aweme_id,
          author: info.author?.nickname,
          author_uid: info.author?.uid,
          digg: info.statistics?.digg_count,
          comment: info.statistics?.comment_count,
          share: info.statistics?.share_count,
          create_time: info.create_time ? new Date(info.create_time * 1000).toLocaleDateString('zh-CN') : null
        }));
      return JSON.stringify({ code: d.code, msg: d.msg, count: videos.length, videos });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

**抖音搜索响应结构**：
```
data.business_data → 对象，key 为 "0","1","2"...
  item.data.aweme_info:
    - desc, aweme_id, create_time
    - author.nickname, author.uid, author.signature
    - statistics.digg_count, statistics.comment_count, statistics.share_count
    - video.play_addr.url_list (视频地址)
    - video.cover.url_list (封面图)
```

#### 2. 小红书笔记搜索（13积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/xhs/note_search?keyword=KEYWORD&sort_type=最多点赞&page=1&page_size=20', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_xhs_search_REQID'
      }
    })
    .then(r => r.json())
    .then(d => {
      const items = d.data?.items || [];
      const notes = items
        .filter(item => item.model_type === 'note' && item.note)
        .map(item => {
          const n = item.note;
          return {
            id: n.id,
            title: n.title,
            desc: n.desc?.substring(0, 100),
            share_url: 'https://www.xiaohongshu.com/explore/' + n.id,
            type: n.type,
            liked_count: n.liked_count,
            collected_count: n.collected_count,
            comments_count: n.comments_count,
            shared_count: n.shared_count,
            user: n.user?.nickname,
            user_id: n.user?.userid,
            timestamp: n.timestamp ? new Date(n.timestamp * 1000).toLocaleDateString('zh-CN') : null
          };
        });
      return JSON.stringify({ code: d.code, msg: d.msg, count: notes.length, notes });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

**小红书搜索响应结构**：
```
data.items → 数组
  item.model_type === "note" 时有效
  item.note:
    - id, title, desc, type ("normal"/"video")
    - liked_count, collected_count, comments_count, shared_count
    - user.nickname, user.userid
    - timestamp, images_list, has_music
    - xsec_token (用于获取详情)
```

#### 3. 抖音视频详情（10积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/douyin/video_detail?aweme_id=AWEME_ID', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_dy_detail_REQID'
      }
    })
    .then(r => r.json())
    .then(d => {
      const info = d.data?.aweme_detail || d.data?.aweme_info;
      return JSON.stringify({
        code: d.code,
        msg: d.msg,
        detail: info ? {
          title: info.desc,
          aweme_id: info.aweme_id,
          share_url: 'https://www.douyin.com/video/' + info.aweme_id,
          create_time: info.create_time ? new Date(info.create_time * 1000).toLocaleDateString('zh-CN') : null,
          author: info.author?.nickname,
          author_uid: info.author?.uid,
          author_signature: info.author?.signature,
          digg: info.statistics?.digg_count,
          comment: info.statistics?.comment_count,
          share: info.statistics?.share_count,
          collect: info.statistics?.collect_count,
          video_url: info.video?.play_addr?.url_list?.[0],
          cover: info.video?.cover?.url_list?.[0],
          duration: info.video?.duration,
          text_extra: info.text_extra?.map(t => t.hashtag_name).filter(Boolean)
        } : null
      });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

**抖音详情响应结构**：
```
data.aweme_detail (或 data.aweme_info):
    - desc, aweme_id, create_time, duration
    - author.nickname, author.uid, author.signature
    - statistics.digg_count, statistics.comment_count, statistics.share_count, statistics.collect_count
    - video.play_addr.url_list, video.cover.url_list, video.duration
    - text_extra[].hashtag_name (话题标签)
```

#### 4. 抖音视频评论（2积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/douyin/video_comments?aweme_id=AWEME_ID&count=20', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_dy_comments_REQID'
      }
    })
    .then(r => r.json())
    .then(d => {
      const comments = d.data?.comments || [];
      const parsed = comments.map(c => ({
        text: c.text,
        user: c.user?.nickname,
        uid: c.user?.uid,
        digg: c.digg_count,
        reply_comment_total: c.reply_comment_total,
        create_time: c.create_time ? new Date(c.create_time * 1000).toLocaleDateString('zh-CN') : null
      }));
      return JSON.stringify({ code: d.code, msg: d.msg, count: parsed.length, comments: parsed });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

**抖音评论响应结构**：
```
data.comments → 数组
  - text, digg_count, create_time
  - user.nickname, user.uid
  - reply_comment_total (回复数)
  - reply_id (用于翻页)
```

#### 5. 抖音账号信息（2积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/douyin/user_info?sec_user_id=SEC_USER_ID', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_dy_userinfo_REQID'
      }
    })
    .then(r => r.json())
    .then(d => {
      const u = d.data?.user;
      return JSON.stringify({
        code: d.code,
        msg: d.msg,
        user: u ? {
          nickname: u.nickname,
          sec_uid: u.sec_uid,
          uid: u.uid,
          signature: u.signature,
          follower_count: u.follower_count,
          following_count: u.following_count,
          aweme_count: u.aweme_count,
          total_favorited: u.total_favorited,
          favoriting_count: u.favoriting_count
        } : null
      });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

**抖音账号信息响应结构**：
```
data.user:
    - nickname, sec_uid, uid, signature
    - follower_count, following_count
    - aweme_count, total_favorited, favoriting_count
    - avatar_larger.url_list
```

#### 6. 抖音账号作品（2积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/douyin/user_videos?sec_user_id=SEC_USER_ID&count=30', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_dy_uservideos_REQID'
      }
    })
    .then(r => r.json())
    .then(d => {
      const list = d.data?.aweme_list || d.data?.list || [];
      const videos = list.map(v => ({
        title: v.desc,
        aweme_id: v.aweme_id,
        share_url: 'https://www.douyin.com/video/' + v.aweme_id,
        create_time: v.create_time ? new Date(v.create_time * 1000).toLocaleDateString('zh-CN') : null,
        digg: v.statistics?.digg_count,
        comment: v.statistics?.comment_count,
        share: v.statistics?.share_count
      }));
      return JSON.stringify({
        code: d.code,
        msg: d.msg,
        count: videos.length,
        has_more: d.data?.has_more,
        max_cursor: d.data?.max_cursor,
        videos
      });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

**抖音账号作品响应结构**：
```
data.aweme_list → 数组
  - desc, aweme_id, create_time
  - statistics.digg_count, statistics.comment_count, statistics.share_count
data.has_more (是否有更多)
data.max_cursor (翻页游标，传入下一次请求的 cursor 参数)
```

#### 7. 小红书笔记详情（10积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/xhs/note_detail?note_id=NOTE_ID', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_xhs_detail_REQID'
      }
    })
    .then(r => r.json())
    .then(d => {
      const n = d.data?.items?.[0]?.note || d.data?.note;
      return JSON.stringify({
        code: d.code,
        msg: d.msg,
        note: n ? {
          id: n.id,
          title: n.title,
          desc: n.desc,
          share_url: 'https://www.xiaohongshu.com/explore/' + n.id,
          type: n.type,
          liked_count: n.liked_count || n.interact_info?.liked_count,
          collected_count: n.collected_count || n.interact_info?.collected_count,
          comments_count: n.comments_count || n.interact_info?.comment_count,
          shared_count: n.shared_count || n.interact_info?.share_count,
          user: n.user?.nickname,
          user_id: n.user?.userid,
          timestamp: n.timestamp ? new Date(n.timestamp * 1000).toLocaleDateString('zh-CN') : null,
          tag_list: n.tag_info?.name ? [n.tag_info.name] : [],
          images: n.images_list?.map(img => img.url_size_large)?.filter(Boolean) || [],
          has_music: n.has_music
        } : null
      });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

#### 8. 小红书笔记评论（13积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/xhs/note_comments?note_id=NOTE_ID&page=1&page_size=20', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_xhs_comments_REQID'
      }
    })
    .then(r => r.json())
    .then(d => {
      const comments = d.data?.comments || [];
      const parsed = comments.map(c => ({
        id: c.id,
        text: c.content,
        user: c.user?.nickname,
        user_id: c.user?.userid,
        like_count: c.like_count,
        sub_comment_count: c.sub_comment_count,
        create_time: c.create_time ? new Date(c.create_time * 1000).toLocaleDateString('zh-CN') : null
      }));
      return JSON.stringify({ code: d.code, msg: d.msg, count: parsed.length, comments: parsed });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

#### 9. 小红书账号信息（13积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/xhs/user_info?user_id=USER_ID', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_xhs_userinfo_REQID'
      }
    })
    .then(r => r.json())
    .then(d => {
      const u = d.data?.user || d.data?.users?.[0];
      return JSON.stringify({
        code: d.code,
        msg: d.msg,
        user: u ? {
          nickname: u.nickname,
          user_id: u.userid || u.user_id,
          signature: u.desc,
          fans: u.fans,
          follows: u.follows,
          interaction: u.interaction,
          note_count: u.note_count
        } : null
      });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

#### 10. 小红书账号作品（13积分）

```javascript
const result = await tools.browser_evaluate({
  script: `
    fetch('/v1/xhs/user_notes?user_id=USER_ID&page=1&page_size=20', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_xhs_usernotes_REQID'
      }
    })
    .then(r => r.json())
    .then(d => {
      const notes = d.data?.notes || d.data?.items || [];
      const parsed = notes.map(n => ({
        id: n.note_id || n.id,
        title: n.display_title || n.title,
        share_url: 'https://www.xiaohongshu.com/explore/' + (n.note_id || n.id),
        type: n.type,
        liked_count: n.liked_count || n.interact_info?.liked_count,
        cover: n.cover?.url || n.images_list?.[0]?.url_size_large
      }));
      return JSON.stringify({
        code: d.code,
        msg: d.msg,
        count: parsed.length,
        has_more: d.data?.has_more,
        notes: parsed
      });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});
text(result.content[0].text);
```

> **模板使用说明**：
> - 将 `USER_API_KEY` 替换为用户提供的 Firefly API Key（首次使用时询问用户获取）
> - 将 `REQID` 替换为唯一的请求 ID（格式：`soso_<模块>_<时间戳>`）
> - 将 `KEYWORD`、`AWEME_ID`、`SEC_USER_ID`、`NOTE_ID`、`USER_ID` 替换为实际参数
> - 抖音搜索用 POST，其他抖音接口用 GET；小红书全部用 GET
> - 翻页：抖音用 `cursor`+`max_cursor`，小红书用 `page`+`page_size`

### 输出格式规范（必读）

> **重要**：所有展示给用户的结果中，每一条视频/笔记都必须包含可点击的原始链接，方便用户直接跳转查看。

**链接生成规则**：
- 抖音视频：`https://www.douyin.com/video/{aweme_id}`
- 小红书笔记：`https://www.xiaohongshu.com/explore/{note_id}`

**API 返回数据要求**：
- 所有搜索/详情/账号作品接口的返回结果中，必须包含 `share_url` 字段
- 模板已内置 `share_url` 生成逻辑，无需额外处理

**结果展示格式要求**：

1. **表格格式**（用于多条件对比、排行榜）：

```markdown
| 排名 | 标题 | 作者 | 点赞 | 链接 |
|------|------|------|------|------|
| 1 | 全屋定制5大避坑 | 锐哥 | 7.4w | [点击观看](https://www.douyin.com/video/xxx) |
| 2 | 板材怎么选 | 罗哥 | 12.7w | [点击观看](https://www.douyin.com/video/xxx) |
```

2. **卡片格式**（用于重点内容展示）：

```markdown
**【板材避坑】全屋定制！该怎么偷工减料？**
- 作者：旭东聊装修 | 点赞：18.3w | 评论：1.3w
- 链接：[点击观看视频](https://www.douyin.com/video/xxx)
```

3. **列表格式**（用于选题清单、推荐列表）：

```markdown
1. **全屋定制5大避坑指南** — 锐哥聊装修 7.4w赞
   👉 [点击观看](https://www.douyin.com/video/xxx)
2. **板材怎么选一分钟说清楚** — 罗哥讲装修 12.7w赞
   👉 [点击观看](https://www.douyin.com/video/xxx)
```

> **注意**：无论是选题挖掘、对标分析还是爆款拆解的输出，每一条内容都必须附带原始链接。链接文字统一用「点击观看」（抖音视频）或「点击查看」（小红书笔记）。

---

## 四大核心模块

### 模块一：选题挖掘

**功能**：基于家居建材/全屋定制/板材赛道，搜索热门内容，分析并生成候选选题清单。

#### 触发
用户说"找选题"、"最近板材/定制什么话题火"、"帮我想想做什么内容"等。

#### 输入信息收集
- 必填：选题方向关键词（如"板材避坑"、"衣柜设计"，或直接说"帮我找全屋定制选题"）
- 选填（缺失用默认值）：
  - 平台：默认取 `default_platform`（推荐小红书）
  - 时间范围：默认"最近一周"
  - 排序：默认"最多点赞"
  - 选题数量：默认 10 个
  - 内容类型：默认不限

#### 智能关键词扩展
如果用户只说"帮我找选题"或"找点板材相关的"，自动使用行业关键词库进行多维度搜索：
- 必搜：板材避坑、全屋定制避坑、板材怎么选
- 可选：根据用户细分方向补充（如"衣柜设计"、"橱柜定制"等）

#### 工作步骤

0. **读取知识库**：
   - 读取 `archive/knowledge/` 下所有文件
   - 加载当前已知选题趋势、方法论洞察
   - 在后续分析中，与知识库对比，标注已知/新发现
1. **确认参数**：和用户确认关键词方向、平台等信息
2. **搜索数据**：调用搜索接口（可多个关键词组合搜索）
   - 抖音：`douyin_video_search`
     - 参数：`keyword`, `sort_type: "1"`（最多点赞）, `publish_time: "7"`（最近一周）
   - 小红书：`xhs_note_search`
     - 参数：`keyword`, `sort_type: "最多点赞"`, `time_filter: "一周内"`
   - 每个关键词取前 20 条结果
3. **数据分析**：
   - 提取标题、描述、话题标签
   - 按行业选题方向分类（避坑类/对比类/科普类/效果类/预算类等）
   - 统计每个方向的热度和数量
   - 识别近期上升趋势的话题
4. **生成报告**：输出结构化的选题清单，分类呈现

#### 输出格式

```
📊 家居建材选题挖掘报告
━━━━━━━━━━━━━━━━
方向：{全屋定制/板材/装修}
平台：{抖音/小红书}
时间范围：最近一周
本次消耗：{X} 积分

🔥 热门选题方向 TOP 10
━━━━━━━━━━━━━━━━

1. 避坑揭秘类｜板材避坑（热度：⭐⭐⭐⭐⭐）
   📌 参考案例：
   • 「全屋定制板材避坑，这5种千万别选」3.2w赞
   • 「板材销售不会告诉你的10个真相」2.8w赞
   💡 创作建议：用"揭秘/真相/千万别"等强钩子词，
      每条配具体案例和替代方案

2. 对比测评类｜颗粒板vs多层板（热度：⭐⭐⭐⭐⭐）
   📌 参考案例：
   • 「颗粒板vs多层板，我终于搞懂了」4.5w赞
   • 「定制衣柜用什么板材？一篇讲透」3.1w赞
   💡 创作建议：分维度对比（环保/价格/防潮/耐用），
      最后给出明确结论和适用场景

3. 科普干货类｜环保等级（热度：⭐⭐⭐⭐）
   ...

4. 预算价格类｜全屋定制多少钱（热度：⭐⭐⭐⭐）
   ...

5. 效果展示类｜全屋实拍（热度：⭐⭐⭐⭐）
   ...

...

💡 行业趋势洞察
━━━━━━━━━━━━━━━━
• 近期"ENF 级板材"搜索量上升明显，环保话题持续升温
• "本地工厂定制"话题增长快，性价比路线受关注
• 小户型收纳类内容互动率高，用户痛点明确
```

5. **存档**：
   a. 询问用户是否存档本次报告
   b. 将选题报告写入 `archive/topics/YYYY-MM-DD_关键词.md`
   c. 提取新选题方向 → 更新 `archive/knowledge/topics-trends.md`
   d. 提取趋势洞察 → 更新 `archive/knowledge/insights.md`
   e. 告知用户："已存档本次选题报告，并更新了知识库（累计 X 个选题方向、Y 条洞察）"

#### 注意事项
- 搜索接口成本较高（13积分/次），可以用 2-3 个核心关键词搜索，覆盖面够了
- 家居行业小红书的内容质量和用户精准度更高，优先搜小红书
- 如果用户需要更细分的选题，可以针对具体空间（衣柜/橱柜/榻榻米）再搜

---

### 模块二：对标账号分析

**功能**：深度分析家居建材/全屋定制/板材类账号的定位、内容策略、数据表现，生成拆解报告。

#### 触发
用户发送账号链接，或说"分析这个账号"、"拆解这个家居博主"、"研究一下对标账号"等。

#### 输入信息收集
- 必填：账号链接 或 账号 ID（sec_user_id / user_id）
- 选填：
  - 平台：自动识别链接，或询问用户
  - 分析深度：基础版（默认，分析 30 条作品）/ 完整版（分析更多）

#### 链接识别
- 抖音链接：包含 `douyin.com` → 平台 = `douyin`
- 小红书链接：包含 `xiaohongshu.com` 或 `xhslink.com` → 平台 = `xhs`
- 无法识别时询问用户

#### 工作步骤

0. **读取知识库**：
   - 读取 `archive/knowledge/` 下所有文件
   - 加载当前已知钩子、结构模板、行业金句
   - 在后续分析中，与知识库对比，标注已知/新发现
1. **识别平台**：根据链接判断平台
2. **获取账号信息**：
   - 抖音：`douyin_user_info`（参数：`sec_user_id`）
   - 小红书：`xhs_user_info`（参数：`user_id` 或 `share_text`）
   - 获取：粉丝数、获赞数、作品数、简介、头像等
3. **获取账号作品**：
   - 抖音：`douyin_user_videos`（参数：`sec_user_id`, `count: 30`）
   - 小红书：`xhs_user_notes`（参数：`user_id` 或 `share_text`）
   - 获取：作品列表（标题、点赞、评论、发布时间等）
4. **深度分析（家居行业专属维度）**：
   - **账号定位**：
     - 赛道细分：板材/全屋定制/装修设计/建材测评/装修日记
     - 人设类型：经销商/设计师/业主/工厂/行业媒体
     - 变现方式：带货/引流到店/培训/广告
     - 目标受众：装修业主/同行/经销商
   - **内容主题分析**：
     - 按行业分类统计：避坑类/对比类/科普类/效果类/预算类/日记类占比
     - 爆款内容集中在哪些方向
   - **产品/品牌分析**：
     - 主推哪些板材品牌/种类
     - 主推哪些五金品牌
     - 价格带定位
   - **发布规律**：发布频率、最佳发布时间
   - **爆款分析**：筛选 TOP 5 高赞作品，分析选题、钩子、结构共性
   - **数据统计**：平均点赞、评论、收藏，数据分布
   - **标题风格**：常用句式、关键词、数字使用、行业术语使用
   - **标签策略**：常用话题标签组合（#全屋定制 #板材 #装修避坑 等）
   - **转化路径**：简介引流、评论区引导、私信话术等
5. **生成报告**：结构化输出 + 可借鉴点 + 差异化建议

#### 输出格式

```
📋 家居账号拆解报告
━━━━━━━━━━━━━━━━
账号：{账号昵称}
粉丝：{粉丝数} | 获赞：{获赞数} | 作品：{作品数}
平台：{抖音/小红书}
本次消耗：{X} 积分

🎯 账号定位
━━━━━━━━━━━━━━━━
赛道：{板材/全屋定制/装修设计}
人设：{经销商/设计师/业主/工厂老板}
身份标签：{如：8年建材老兵 / 自装两套房主}
slogan/简介：{简介内容}
目标受众：{装修业主/同行经销商/设计师}
变现方式：{引流到店/带货/培训/广告}
价格带定位：{中低端/中端/中高端/高端}

📊 内容分析
━━━━━━━━━━━━━━━━
主题分布：
• 避坑揭秘类：{占比}% ← 流量担当
• 对比测评类：{占比}% ← 收藏担当
• 科普干货类：{占比}% ← 专业担当
• 效果展示类：{占比}% ← 视觉担当
• 预算价格类：{占比}% ← 精准客户
• 装修日记类：{占比}% ← 人设担当

主推产品/品牌：
• 板材：{品牌/种类列表}
• 五金：{品牌列表}
• 价格带：{价格范围}

发布规律：
• 频率：{日更/周更X条}
• 最佳发布时间：{时间段}
• 发布数量趋势：{上升/稳定/下降}

🔥 爆款分析 TOP 5
━━━━━━━━━━━━━━━━
1. 《{标题}》 {点赞数}赞 {评论数}评
   选题类型：{避坑/对比/科普...}
   核心钩子：{钩子内容}
   发布时间：{时间}
   为什么火：{分析}
   可复制点：{可借鉴的地方}

2. ...

💡 可借鉴点
━━━━━━━━━━━━━━━━
• {借鉴点1：比如"标题多用数字+避坑，点击率高"}
• {借鉴点2：比如"前3秒直接抛痛点，完播率高"}
• {借鉴点3：比如"评论区引导提问，互动率高"}

🎯 差异化建议
━━━━━━━━━━━━━━━━
• {建议1：对方没覆盖的选题方向}
• {建议2：可以打造的差异化人设}
• {建议3：内容形式上的创新点}
```

#### 注意事项
- 抖音账号分析成本很低（4积分），可以多分析几个对标账号
- 小红书账号分析成本较高（26积分），确认用户需求后再调用
- 家居行业建议优先分析小红书账号，用户更精准
- **重要**：user_videos 每次最多返回 30 条，且按时间倒序排列。旧爆款可能不在第一页。建议至少翻页 2 次（获取 60 条），确保覆盖到历史爆款。翻页时传入上一次返回的 `max_cursor` 参数。
- **跨关键词去重**：多个关键词搜索的结果可能有重复，分析前先按 aweme_id/note_id 去重

6. **存档**：
   a. 询问用户是否存档本次报告
   b. 将账号报告写入 `archive/accounts/YYYY-MM-DD_账号名.md`
   c. 提取新钩子/结构 → 更新 `archive/knowledge/hooks.md`、`structures.md`
   d. 提取新金句 → 更新 `archive/knowledge/quotes.md`
   e. 提取账号策略洞察 → 更新 `archive/knowledge/insights.md`
   f. 告知用户："已存档账号分析报告，并更新了知识库"

---

### 模块三：爆款结构拆解

**功能**：拆解多条家居类爆款内容的结构、钩子、节奏，提炼可复用模板。

#### 触发
用户说"拆解这个爆款"、"分析这个视频为什么火"、"帮我找家居爆款规律"等，或发送多条爆款链接。

#### 输入信息收集
- 必填：3-5 条爆款内容链接
- 选填：
  - 拆解维度：默认全维度
  - 是否分析评论：默认是（抖音评论便宜必拿，小红书按需）

#### 链接识别
同模块二，根据链接域名判断平台。

#### 工作步骤

0. **读取知识库**：
   - 读取 `archive/knowledge/` 下所有文件
   - 加载当前已知钩子、结构、金句、选题趋势、方法论洞察
   - 在后续拆解中，与知识库对比，标注"已知"或"NEW"
1. **确认样本**：收集 3-5 条爆款链接
2. **批量获取详情**：
   - 抖音：`douyin_video_detail`（每条调用一次，参数 `share_url`）
   - 小红书：`xhs_note_detail`（每条调用一次，参数 `share_text`）
   - 获取：标题、描述/正文、话题标签、发布信息、互动数据等
3. **批量获取评论**（可选）：
   - 抖音：`douyin_video_comments`（2积分/条，低成本，推荐获取）
   - 小红书：`xhs_note_comments`（13积分/条，成本较高，按需获取）
   - 取热门评论前 20 条
4. **多维度拆解分析（家居行业专属）**：
   - **高频选题方向**：
     - 按行业分类：避坑/对比/科普/效果/预算/日记
     - 统计各类型占比
     - 找共性选题角度
   - **开头钩子分析**：
     - 提取前 3 秒 / 前 30 字
     - 归类钩子类型（家居行业常见类型）：
       - 数字避坑式："全屋定制 10 大坑，第 5 个最容易踩"
       - 提问痛点式："全屋定制到底选颗粒板还是多层板？"
       - 反差揭秘式："别再被销售忽悠了，ENF 级根本不是最好的"
       - 利益承诺式："这样选板材，至少省 2 万块"
       - 身份背书式："做了 8 年建材，告诉你板材怎么选"
     - 分析哪种钩子效果最好
   - **内容结构拆解**：
     - 划分段落结构
     - 提炼家居行业常用结构模板：
       - 避坑清单型：X 个坑 → 每个坑说清楚 + 怎么避
       - 对比测评型：A vs B → 分维度对比 → 结论建议
       - 科普讲解型：是什么 → 为什么 → 怎么选
       - 流程步骤型：从 X 到 Y → 一步一步讲
       - 效果展示型：before → 过程 → after 展示
     - 分析节奏和信息密度
   - **行业术语/金句提取**：
     - 常用的行业术语和表达方式
     - 容易引起共鸣的句子
     - 记忆点强的金句
   - **评论区热点分析**：
     - 用户最关心的问题（比如"多少钱"、"什么品牌"、"环保吗"）
     - 评论区高频疑问（可作为后续选题）
     - 用户痛点和抗拒点
   - **结尾引导分析**：
     - CTA 方式（引导关注/点赞/评论/收藏/私信）
     - 引流话术（怎么引导到店/加微信）
5. **提炼可复用模板**：
   - 结构模板（2-3 种家居行业常用结构）
   - 钩子模板（按类型整理，带家居行业示例）
   - 选题模板（高频选题方向 + 变体方法）

#### 输出格式

```
🔬 家居爆款结构拆解报告
━━━━━━━━━━━━━━━━
分析样本：{N} 条爆款内容
平台：{抖音/小红书}
本次消耗：{X} 积分

🎯 高频选题方向
━━━━━━━━━━━━━━━━
1. 避坑揭秘类（{X/N}条）← 流量最高
   关键词：避坑、千万别、真相、套路、揭秘、不会告诉你
   热门角度：板材避坑、五金避坑、计价避坑、环保避坑

2. 对比测评类（{X/N}条）← 收藏最高
   关键词：vs、对比、区别、哪个好、怎么选
   热门角度：颗粒板vs多层板、E0vsENF、品牌vs工厂

3. 科普干货类（{X/N}条）← 专业人设
   关键词：一篇讲透、怎么选、科普、解析、入门
   热门角度：环保等级、五金知识、板材种类

4. ...

🪝 开头钩子分析
━━━━━━━━━━━━━━━━
类型分布：
• 数字避坑式：{占比}%
  例："全屋定制 10 大坑，第 3 个 90% 的人都踩过"
  效果：完播率高，用户想知道自己有没有踩坑

• 提问痛点式：{占比}%
  例："定制衣柜到底用颗粒板还是多层板？纠结死了"
  效果：互动率高，用户会评论自己的选择

• 反差揭秘式：{占比}%
  例："别再被销售忽悠了，ENF 级板材根本不是最好的"
  效果：点击率高，利用认知反差吸引点击

• 利益承诺式：{占比}%
  例："这样选全屋定制，至少省 3 万块"
  效果：精准吸引预算敏感型用户

• 身份背书式：{占比}%
  例："做了 8 年建材销售，告诉你板材怎么选不被坑"
  效果：信任度高，专业人设强

效果最佳：{类型}（对应爆款数据最好）

📐 可复用结构模板
━━━━━━━━━━━━━━━━

【模板 A：避坑清单型】
适用场景：板材避坑、定制避坑、装修避坑类内容

结构（60秒视频/千字图文）：
1. 开头钩子（0-3s/前30字）
   → 数字+避坑："全屋定制 X 大坑，第 X 个最容易踩"
   → 或提问："你家全屋定制踩坑了吗？"
2. 引入（3-10s）
   → 强化痛点："90% 的人第一次装修都会踩这些坑"
   → 身份背书："做了 X 年建材，今天一次性说清楚"
3. 主体（10-45s）
   → 分 X 个坑点（建议 3-5 个）
   → 每个坑点：是什么坑 → 为什么坑 → 怎么避
   → 重要的放前面，次重要的放后面
4. 结尾（45-60s）
   → 总结："记住这 X 点，定制不踩坑"
   → CTA："收藏起来，装修的时候照着看"
   → 互动："你们踩过哪个坑？评论区聊聊"

【模板 B：对比测评型】
适用场景：板材对比、品牌对比、计价方式对比

结构：
1. 开头钩子
   → 提问式："A 和 B 到底选哪个？今天一次性讲透"
   → 或痛点式："90% 的人都在这个问题上纠结"
2. 引入
   → 说明对比维度："今天从 X 个维度对比，看完你就知道怎么选了"
3. 主体
   → 维度1：环保性 → A 怎么样 → B 怎么样
   → 维度2：价格 → A 怎么样 → B 怎么样
   → 维度3：耐用性 → A 怎么样 → B 怎么样
   → 维度4：适用场景 → A 适合什么 → B 适合什么
4. 结论
   → 明确结论："如果是 XX 情况，选 A；如果是 XX 情况，选 B"
   → 给出明确建议，不要模棱两可
5. 结尾
   → CTA："还有什么想对比的？评论区告诉我"
   → 关注引导："关注我，选材不纠结"

【模板 C：科普讲解型】
适用场景：环保等级、板材知识、五金知识

结构：
1. 开头钩子
   → 痛点式："还在傻傻分不清 E0 和 ENF？别再被销售忽悠了"
   → 或提问式："你知道板材环保等级怎么看吗？"
2. 引入
   → "今天一篇讲透，看完你比销售还懂"
3. 主体
   → 是什么：概念解释（用大白话，别太专业）
   → 为什么：为什么重要（和用户有什么关系）
   → 怎么选：给出具体的选购建议
   → 配案例/数据：用具体数字增强说服力
4. 总结
   → 一句话总结核心要点
5. 结尾
   → CTA："收藏起来，选板材的时候拿出来看"
   → 互动："还有什么想了解的？评论区问我"

💎 行业金句/亮点库
━━━━━━━━━━━━━━━━
• "选板材，环保是底线，不是加分项" —— 环保类内容
• "贵的不一定对，但太便宜的一定有问题" —— 价格类内容
• "五金是定制的灵魂，别在这上面省钱" —— 五金类内容
• "投影面积就是耍流氓，展开面积才是实诚" —— 计价类内容
• "ENF 级不是终点，是及格线" —— 环保等级内容
• "颗粒板不是差，只是适用场景不同" —— 板材科普内容

💬 评论区热点（选题金矿）
━━━━━━━━━━━━━━━━
用户最关心的问题：
• "XX 品牌怎么样？" —— 可以做品牌测评选题
• "多少钱一平方？" —— 可以做价格/预算选题
• "小户型适合什么板材？" —— 可以做场景化选题
• "有没有推荐的品牌？" —— 可以做推荐类选题
• "环保等级怎么查？" —— 可以做科普类选题

（这些都是用户真实需求，可以直接作为后续选题方向）
```

#### 注意事项
- 建议至少 3 条爆款一起分析，才能找到共性规律
- 抖音评论接口很便宜（2积分/条），建议必拿，评论区有大量选题金矿
- 小红书评论较贵，按需获取
- 家居行业评论区的"多少钱"、"什么品牌"是高频问题，也是高转化的选题方向

6. **存档**：
   a. 询问用户是否存档本次报告
   b. 将拆解报告写入 `archive/breakdowns/YYYY-MM-DD_N条样本.md`
   c. 更新知识库：
      - 新钩子类型 → `archive/knowledge/hooks.md`（标注 NEW）
      - 新结构模板 → `archive/knowledge/structures.md`（标注 NEW）
      - 新行业金句 → `archive/knowledge/quotes.md`（标注 NEW）
      - 新选题方向 → `archive/knowledge/topics-trends.md`
      - 新方法论洞察 → `archive/knowledge/insights.md`
   d. 告知用户："已存档拆解报告。本次新增 X 个钩子、Y 个结构、Z 条金句。知识库累计：N 种钩子、M 种结构、K 条金句"

---

### 模块四：内容初稿生成

**功能**：基于选题、爆款模板和用户人设，生成家居建材/全屋定制/板材类内容初稿。

#### 触发
用户说"帮我写个脚本"、"生成一篇初稿"、"照着这个模板写一篇板材内容"等。

#### 输入信息收集
- 必填：
  - 选题方向（如"板材避坑"、"颗粒板vs多层板"）
  - 选择结构模板（避坑清单型 / 对比测评型 / 科普讲解型 / 效果展示型）
- 选填（从配置中读取，缺失则询问）：
  - 人设信息（身份、风格、口头禅、经历等）
  - 主推产品/品牌信息（代理的板材品牌、五金品牌等）
  - 目标受众
  - 内容形式（口播脚本 / 小红书图文 / 抖音文案）
  - 目标时长/字数

#### 工作步骤

1. **确认需求**：选题、模板、形式、时长
2. **（可选）获取参考**：如果用户提供了参考链接，调用详情接口获取内容
3. **加载配置**：从对话记忆中读取人设、品牌、产品等配置
4. **生成初稿**：
   - 按照选定的结构模板填充
   - 注入人设风格（语气、用词、个人经历、身份标签）
   - 融入品牌/产品信息（自然植入，不生硬）
   - 适配目标平台调性（抖音 vs 小红书）
   - 加入具体案例和数据（价格、参数、品牌等）
   - 使用行业术语体现专业度（但要用大白话解释）
5. **输出初稿 + 优化建议**

#### 输出格式（口播脚本示例）

```
✍️ 内容初稿
━━━━━━━━━━━━━━━━
选题：全屋定制板材避坑指南
模板：避坑清单型
形式：60秒口播脚本
人设：8年建材经销商，直爽实在风格

【开头钩子（0-3s）】
（镜头：对着镜头，表情认真）
"全屋定制 5 大坑，第 3 个 90% 的人都踩过！"

【引入（3-10s）】
"做了 8 年板材生意，见过太多业主被坑。
今天我把最常见的 5 个坑说出来，
看完至少帮你省两万。
点赞收藏，装修的时候照着避。"

【坑1：板材环保等级（10-20s）】
"第一个坑，环保等级。
很多销售一上来就跟你说'我们家是 E0 级的，最环保'。
别信！现在最高等级是 ENF 级，
E0 级已经是入门级了。
而且一定要看检测报告，别光听嘴说。
（举检测报告画面）
记住，甲醛释放量越低越好，
ENF 级 ≤ 0.025mg/m³，比 E0 级高一倍多。"

【坑2：计价方式（20-30s）】
"第二个坑，计价方式。
投影面积听起来便宜，做下来贵得要死。
什么是投影面积？就是宽×高，
听起来简单，但里面猫腻多了。
加隔板加钱，加抽屉加钱，加挂衣杆加钱，
最后加出来的钱能吓死你。
想要不被坑，就按展开面积算，
用多少算多少，明明白白。"

【坑3：五金件（30-40s）】
"第三个坑，也是最容易被忽略的——五金。
很多人关注板材，不关注五金，
但五金才是决定柜子使用寿命的关键。
铰链用差了，用两年就掉门；
滑轨用差了，抽屉拉不开。
别在五金上面省钱，
认准百隆、海蒂诗这些品牌，
多花几百块，多用好几年。"

【坑4+坑5+结尾（40-60s）】
"第四个坑，板材厚度。
柜体一定要用 18mm 的，
别用 16mm 的，差不了多少钱，
但结实程度差很多。
第五个坑，背板。
很多商家背板用 5mm 的，
一定要让商家换成 9mm 的，
防潮效果好太多。

记住这 5 个坑，全屋定制不踩坑。
还有什么想了解的，评论区问我。
关注我，一个说实话的建材人。"

---
💡 优化建议
━━━━━━━━━━━━━━━━
• 开头可以再加一个动作（比如伸手比"5"），更有冲击力
• 每个坑点配对应的产品画面/对比图，视觉效果更好
• 评论区可以置顶"需要板材避坑清单的，评论区打'想要'"，引导互动
• 如果有实体门店，可以在简介或评论区引导到店
```

#### 小红书图文格式说明
如果是小红书图文，结构调整为：
- **标题**：带数字+关键词+情绪词（如"全屋定制5大坑！第3个90%的人都踩过😭"）
- **封面图**：大字报风格，核心信息一目了然
- **正文**：分段清晰，多用 emoji，每段不要太长
- **标签**：#全屋定制 #板材 #装修避坑 #定制衣柜 #家居 等 8-15 个标签

#### 注意事项
- 初稿生成不消耗 API 积分（纯 LLM 生成）
- 如果用户对初稿不满意，可以调整风格、增减内容、更换角度后重新生成
- 鼓励用户在初稿基础上加入真实案例、个人经历、门店信息，提高原创性和转化效果
- 涉及具体品牌、价格时，提醒用户根据实际情况调整

### 三轮优化流程（初稿生成后自动推荐）

初稿生成后，主动向用户推荐三轮优化，用户可以选择全部做或只做某一轮：

```
初稿 → 第一轮：钩子优化（dbs-hook）→ 第二轮：内容诊断（dbs-content）→ 第三轮：逐字稿流畅度（dbs-script-flow）
```

#### 第一轮：钩子优化（调用 dbs-hook Skill）

**目的**：优化开头前 5 秒，降低开头流失率

**触发**：初稿生成后，问用户：「要不要优化开头钩子？我可以用 dbs-hook 帮你诊断开头，生成 10-15 条优化方案。」

**调用方式**：
```
Use Skill: dbs-hook

【把初稿的开头部分（前2-3句）和完整正文传给 dbs-hook】
```

**dbs-hook 做什么**：
1. 先诊断内容素材够不够（数据、故事、金句、权威、痛点）
2. 如果有开头，诊断当前开头的 6 个维度（独立性/Hook/悬念/可信度/口播友好/匹配度）
3. 用三种方法生成 10-15 条开头方案：
   - 素材提取法（从文案里挖已有素材）
   - 素材增补法（主动增补真实数据/案例）
   - 悬念制造法（把结论改成问题）
4. 给出 Top 3 推荐和理由

**产出**：开头优化方案（10-15 条 + Top 3 推荐）

**对接方式**：用户选好开头后，把新开头替换回初稿，然后进入第二轮。

---

#### 第二轮：内容诊断（调用 dbs-content Skill）

**目的**：五维诊断内容质量，确保内容有价值、有落差、能打动人

**触发**：钩子优化完成后（或用户跳过第一轮），问用户：「要不要做内容诊断？我可以用 dbs-content 帮你从五个维度检查内容质量。」

**调用方式**：
```
Use Skill: dbs-content

【把完整初稿和选题方向传给 dbs-content，说明是短视频/图文形式】
```

**dbs-content 做什么**：
1. 确认内容形式和平台匹配度
2. 五维诊断：
   - 文字洁癖：有没有 AI 味？语言干不干净？
   - 封面/标题：有没有吸引力？是不是认知劫持？
   - 表达效率：有没有冗余？是不是在自嗨？
   - 认知落差：和同行比有没有差异化？
   - AI 辅助：推荐最合适的 AI 工作流
3. 给出具体的第一步行动

**产出**：五维诊断报告 + 具体行动建议

**对接方式**：根据诊断结果调整初稿（比如清洗 AI 味、优化标题、精简冗余），然后进入第三轮。

---

#### 第三轮：逐字稿流畅度检查（调用 dbs-script-flow Skill）

**目的**：检查段落衔接、信息密度和口播流畅度，找到观众会划走的位置

**触发**：内容诊断完成后（或用户跳过第二轮），问用户：「要不要检查逐字稿流畅度？我可以用 dbs-script-flow 帮你找出观众可能划走的位置。」

**调用方式**：
```
Use Skill: dbs-script-flow

【把完整逐字稿传给 dbs-script-flow】
```

**dbs-script-flow 做什么**：
1. 把逐字稿按话题分段，标注每段功能
2. 逐段扫描三个维度：
   - 逻辑衔接：前后段之间有没有断裂？
   - 信息密度：有没有重复/绕圈子/正确的废话？
   - 口播流畅度：句子太长？书面语？自问自答？
3. 标注风险等级：🔴高风险 / 🟡中风险 / 🟢小建议
4. 每个问题给出具体修复建议
5. 用户确认后可执行标记式改稿（保留原文，用删除线+🆕标注改动）

**产出**：逻辑延续检查报告 + 可选的标记式改稿

**对接方式**：用户确认改稿后，得到最终优化版逐字稿。

---

#### 三轮优化后的最终稿

三轮优化全部完成后：
1. 整合所有优化点，生成最终版初稿
2. 询问用户是否存档
3. 存档时将优化过程也一并记录（开头用了哪条、诊断出了什么问题、改了哪些地方）

#### 优化流程的灵活度

- 用户可以跳过任意一轮，直接进入下一轮
- 用户可以只做某一轮（比如只优化开头）
- 每轮完成后都问用户：「继续下一轮吗？」
- 如果用户说"够了"，就停止优化，进入存档

6. **存档（可选）**：
   - 用户确认初稿满意后，将初稿存入 `archive/drafts/YYYY-MM-DD_选题.md`
   - 提取初稿中的有效表达 → 更新 `archive/knowledge/quotes.md`
   - 如果经过了三轮优化，记录优化过程和结果
   - 告知用户："已存档初稿，可随时回看"

---

## 串联工作流（完整方案）

当用户说"帮我做一套自媒体方案"、"从选题到出稿帮我搞定"时，按以下顺序串联四大模块：

```
1. 选题挖掘 → 选 1-2 个方向 → 【存档选题报告 + 更新趋势库】
      ↓
2. 对标分析 → 找 2-3 个对标账号 → 【存档账号报告 + 更新钩子/结构库】
      ↓
3. 爆款拆解 → 挑 3-5 条爆款 → 【存档拆解报告 + 全面更新知识库】
      ↓
4. 初稿生成 → 基于模板生成 → 【可选存档初稿】
      ↓
5. 三轮优化（可选）：
   第一轮：钩子优化（dbs-hook）→ 10-15条开头方案 + Top3推荐
   第二轮：内容诊断（dbs-content）→ 五维诊断 + 行动建议
   第三轮：逐字稿流畅度（dbs-script-flow）→ 流失点检查 + 标记式改稿
      ↓
   最终稿 → 【可选存档 + 记录优化过程】
```

### 完整工作流步骤

1. **需求确认**：
   - 确认细分赛道（板材/全屋定制/装修）
   - 确认目标平台（推荐小红书）
   - 确认人设和目标受众
2. **第一站：选题挖掘**
   - 搜索 2-3 个核心方向的热门选题
   - 输出 10 个选题方向，分类呈现
   - 让用户选择 1-2 个感兴趣的方向
3. **第二站：对标分析**
   - 基于选定方向，搜索或用户提供 2-3 个对标账号
   - 逐个分析账号定位、内容策略、爆款特点
   - 总结可借鉴点和差异化机会
4. **第三站：爆款拆解**
   - 从对标账号的爆款中选 3-5 条代表性作品
   - 拆解结构、钩子、选题、评论热点
   - 提炼 2-3 个可复用模板
5. **第四站：初稿生成**
   - 选择一个模板和选题
   - 注入用户人设和品牌信息
   - 生成内容初稿
   - 给出优化建议
6. **总结**：汇总所有产出，给出后续运营建议

### 成本预估
以小红书为例（家居行业推荐），完整走一轮大约消耗：
- 选题挖掘（2个关键词）：13 × 2 = 26 积分
- 对标分析（2个账号）：(13+13) × 2 = 52 积分
- 爆款拆解（5条，含评论）：(10+13) × 5 = 115 积分
- 初稿生成：0 积分
- **合计：约 193 积分 ≈ ¥1.93**

以抖音为例，完整走一轮大约消耗：
- 选题挖掘（2个关键词）：13 × 2 = 26 积分
- 对标分析（2个账号）：(2+2) × 2 = 8 积分
- 爆款拆解（5条，含评论）：(10+2) × 5 = 60 积分
- 初稿生成：0 积分
- **合计：约 94 积分 ≈ ¥0.94**

> 抖音成本约为小红书的一半，但小红书家居用户更精准，建议按需选择。

---

## 存档与知识进化

Skill 的核心能力之一：**从实战中持续学习，越用越强。**

每次完成选题挖掘、对标分析、爆款拆解后，自动将拆解成果存档到 `archive/` 目录，并更新知识库。下次使用时加载积累的知识，不再从零开始。

### 存档目录结构

```
Soso-self-media/
└── archive/
    ├── README.md                # 存档说明
    ├── topics/                  # 选题挖掘存档
    │   └── YYYY-MM-DD_关键词.md
    ├── accounts/                # 对标账号存档
    │   └── YYYY-MM-DD_账号名.md
    ├── breakdowns/               # 爆款拆解存档
    │   └── YYYY-MM-DD_N条样本.md
    ├── drafts/                   # 初稿存档（可选）
    │   └── YYYY-MM-DD_选题.md
    └── knowledge/                # 进化知识库（随拆解自动更新）
        ├── hooks.md              # 钩子类型库（初始5种 → 持续增长）
        ├── structures.md         # 结构模板库（初始4种 → 持续增长）
        ├── quotes.md             # 行业金句库
        ├── topics-trends.md      # 选题趋势库
        └── insights.md           # 方法论洞察库
```

### 存档触发条件

| 模块 | 触发时机 | 存档内容 | 存档路径 |
|------|----------|----------|----------|
| 选题挖掘 | 生成选题报告后 | 热门选题、趋势洞察、关键词效果 | `archive/topics/` |
| 对标分析 | 生成账号报告后 | 账号定位、内容策略、爆款共性 | `archive/accounts/` |
| 爆款拆解 | 生成拆解报告后 | 新钩子、新结构、金句、评论热点 | `archive/breakdowns/` |
| 初稿生成 | 用户确认满意后 | 完整初稿 | `archive/drafts/` |

### 存档流程（每次拆解完成后执行）

1. **存档本次结果**：将完整报告写入 archive/ 对应目录
   - 命名规则：`YYYY-MM-DD_关键词或账号名.md`
   - 格式：标准 Markdown，含 YAML 元数据头（日期、平台、消耗积分、样本数）
2. **提取新知识**：从拆解结果中提取新的规律性内容
   - 新钩子类型 → 更新 `archive/knowledge/hooks.md`（标注 NEW）
   - 新结构模板 → 更新 `archive/knowledge/structures.md`（标注 NEW）
   - 新行业金句 → 更新 `archive/knowledge/quotes.md`（标注 NEW）
   - 新选题趋势 → 更新 `archive/knowledge/topics-trends.md`
   - 新方法论洞察 → 更新 `archive/knowledge/insights.md`
3. **更新计数**：在各知识库文件头部更新"最后更新日期"和"已积累数量"

### 知识库读取规则（每次分析前执行）

0. **读取知识库**：
   - 读取 `archive/knowledge/` 下所有文件
   - 加载当前已知钩子、结构、金句、选题趋势、方法论洞察
   - 在后续分析中，与知识库对比：
     - 已有条目标注「已知」
     - 新发现标注「NEW」并在存档时更新知识库

### 存档文件格式

```yaml
---
date: 2026-08-23
platform: douyin
cost: 32
sample_count: 3
---

# 报告标题
（完整报告内容）
```

### 存档话术

- 分析前：「正在加载知识库...（已积累 X 种钩子、Y 种结构、Z 条金句）」
- 分析后：「本次发现 N 个新钩子、M 个新结构。是否存档并更新知识库？」
- 存档后：「已存档。知识库累计：N 种钩子、M 种结构、K 条金句、L 条洞察」

---

## 用户配置管理

### 配置项清单

| 配置项 | 说明 | 何时需要 | 默认值 |
|--------|------|----------|--------|
| `api_key` | Firefly API Key | 首次调用 API 前 | 需用户提供 |
| `default_platform` | 默认使用平台 | 选题挖掘等 | `xhs` |
| `industry` | 细分赛道 | 全模块 | `全屋定制/板材` |
| `persona` | 人设信息（身份、风格、经历） | 生成初稿时 | 无（推荐配置） |
| `target_audience` | 目标受众 | 生成初稿时 | 装修业主 |
| `brand_info` | 代理/主推的品牌、产品信息 | 生成初稿时 | 无 |
| `content_style` | 内容风格偏好 | 生成初稿时 | 无 |
| `common_topics` | 常用选题方向 | 选题挖掘默认关键词 | 板材避坑、全屋定制 |
| `competitor_accounts` | 关注的对标账号列表 | 快捷分析 | 无 |

### 配置记忆规则

- 用户在对话中提供的配置信息，**在当前对话中持续有效**
- 每次需要配置项时，先检查对话历史中是否已有
- 如果配置有更新，以最新提供的为准
- 不要假设用户有配置，缺失时主动询问
- 可以主动询问用户是否需要配置某项（特别是人设和品牌信息，对生成质量影响很大）

### 首次配置引导话术

> "在开始之前，我需要了解你的一些信息（只需要说一次，之后我都记住）：
> 1. 你的 Firefly API Key 是什么？（如果没有，需要先注册 firefly.qwjxqn.xyz 获取）
> 2. 你主要做抖音还是小红书？（家居行业推荐小红书，用户更精准）
> 3. 方便介绍一下你的账号人设吗？比如你是板材经销商/设计师/装修业主，风格是怎样的？
> 4. 你主要做什么产品/品牌？（比如代理什么板材品牌、做定制还是卖材料）"

---

## 错误处理指南

### API 调用错误处理

| 情况 | 处理方式 |
|------|---------|
| 浏览器未导航到 Firefly | 先导航到 `https://firefly.qwjxqn.xyz/docs/agent-api`，再重试 |
| 密钥无效（1401） | "API Key 无效，请检查后重新提供" |
| 积分不足（1402） | "积分不足了，当前余额不足本次调用。可以去 Firefly 控制台充值后再试" |
| 请求频繁（1429） | 自动等待 2 秒后重试（最多重试 2 次），仍失败则提示稍后再试 |
| 内容不存在（2404） | "这条内容暂时查不到，可能是链接不对或者还没被收录，换一条试试？" |
| 上游错误（1502/1503/1504） | "数据源暂时有点不稳定，稍等一会儿再试试？" |
| 评论不可用（1001） | "这条内容的评论暂时获取不到，可能是评论已关闭。跳过评论分析，仅用详情数据做拆解" |

### 幂等键规则

每次 API 调用都传入 `request_id`：
- 格式：`soso_<模块名>_<unix时间戳>_<随机3位>`
- 示例：`soso_topic_1724342400_a1b`
- 同一参数重试时使用相同的 request_id，避免重复扣费

---

## 成本意识最佳实践

1. **优先使用低成本接口**：
   - 抖音的账号信息/作品/评论只要 2 积分，大胆用
   - 小红书同类接口 13 积分，按需使用
   - 先在抖音上找对标账号（便宜），确定后再去小红书分析（精准）

2. **搜索接口省着用**：
   - 搜索 13 积分/次，一次搜够量（取 20 条以上）
   - 先用行业关键词库中的核心词搜索，2-3 个就够
   - 尽量通过"账号作品"接口批量获取内容，比搜索划算

3. **小红书 vs 抖音选择**：
   - 家居行业小红书用户更精准，但成本高 3-5 倍
   - 建议：先抖音找对标（便宜），再小红书深扒爆款（精准）
   - 或者根据目标客户选择：To C 业主选小红书，To B 同行选抖音

4. **提前确认需求**：
   - 调用前确认用户真的需要，避免浪费
   - 可以先告知大概消耗多少积分，让用户心里有数

5. **失败不扣费，放心重试**：
   - Firefly 失败请求不扣积分
   - 用幂等键防止重复扣费

---

## 常用话术模板

### 开始分析前
> "好的，我来帮你{做什么}。这次分析大约消耗 {X} 积分，我们开始吧？"

### 分析完成后
> "分析完成！本次消耗 {X} 积分。以下是分析结果："

### 询问用户选择
> "以上是 {N} 个选题方向，你对哪个比较感兴趣？我可以继续深入拆解或者直接写初稿。"

### 引导下一步
> "接下来要不要{下一步动作}？比如{具体建议}"

### 家居行业专属话术
> "从评论区来看，大家最关心的还是价格和环保问题，这两个方向可以重点做，很容易出爆款。"
> "这个账号的人设跟你挺像的，他的爆款结构你可以直接套用，然后加入你自己的产品和案例。"

---

## 平台差异速查

| 能力 | 抖音接口路径 | 小红书接口路径 | 抖音积分 | 小红书积分 |
|------|-------------|-------------|---------|-----------|
| 搜索 | `/v1/douyin/video_search` | `/v1/xhs/note_search` | 13 | 13 |
| 详情 | `/v1/douyin/video_detail` | `/v1/xhs/note_detail` | 10 | 10 |
| 评论 | `/v1/douyin/video_comments` | `/v1/xhs/note_comments` | 2 | 13 |
| 账号信息 | `/v1/douyin/user_info` | `/v1/xhs/user_info` | 2 | 13 |
| 账号作品 | `/v1/douyin/user_videos` | `/v1/xhs/user_notes` | 2 | 13 |

**家居行业平台选择建议**：
- **小红书**：用户精准（装修业主多）、转化高、内容生命周期长，但成本高
- **抖音**：流量大、成本低、适合做品牌曝光，但用户精准度稍低
- **推荐策略**：小红书做深度内容（种草+转化），抖音做流量内容（曝光+引流）

---

## 积分类型注意事项

> **重要**：Firefly 积分有多种来源（注册赠送、活动赠送、充值），不同来源的积分可能有使用限制。

- **充值积分**（体验包/标准包等）：全功能可用，无限制
- **注册赠送积分**（200积分）：全功能可用，但数量有限
- **活动/专属赠送积分**（如 HermesAI 赠送）：可能仅适用于低成本接口（2积分/次），搜索类接口（13积分/次）可能无法使用
- **如果搜索接口返回 1402（积分不足）但余额充足**：很可能是积分类型限制，需要充值后才能使用搜索接口
- **充值方式**：访问 `https://firefly.qwjxqn.xyz/pricing`，体验包 ¥10 = 1000 积分（限购1次）

---

## 完整调用示例（选题挖掘）

以下是从初始化到输出选题报告的完整 Exec 调用流程：

```javascript
// Step 1: 初始化导航
await tools.browser_navigate({ url: 'https://firefly.qwjxqn.xyz/docs/agent-api' });
await tools.browser_wait_for({ time: 2 });

// Step 2: 抖音搜索（板材避坑方向）
const dyResult = await tools.browser_evaluate({
  script: `
    fetch('/v1/douyin/video_search', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_topic_dy_001'
      },
      body: JSON.stringify({ keyword: '板材避坑', sort_type: '1', publish_time: '0', count: '20' })
    })
    .then(r => r.json())
    .then(d => {
      const bd = d.data?.business_data || {};
      const videos = Object.values(bd)
        .map(item => item?.data?.aweme_info)
        .filter(info => info)
        .map(info => ({
          title: info.desc, author: info.author?.nickname,
          share_url: 'https://www.douyin.com/video/' + info.aweme_id,
          digg: info.statistics?.digg_count, comment: info.statistics?.comment_count
        }));
      return JSON.stringify({ code: d.code, count: videos.length, videos });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});

// Step 3: 小红书搜索（板材避坑方向）
const xhsResult = await tools.browser_evaluate({
  script: `
    fetch('/v1/xhs/note_search?keyword=板材避坑&sort_type=最多点赞&page=1&page_size=20', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer USER_API_KEY',
        'X-Request-Id': 'soso_topic_xhs_001'
      }
    })
    .then(r => r.json())
    .then(d => {
      const items = d.data?.items || [];
      const notes = items
        .filter(item => item.model_type === 'note' && item.note)
        .map(item => {
          const n = item.note;
          return {
            title: n.title, user: n.user?.nickname,
            share_url: 'https://www.xiaohongshu.com/explore/' + n.id,
            liked: n.liked_count, collected: n.collected_count
          };
        });
      return JSON.stringify({ code: d.code, count: notes.length, notes });
    })
    .catch(err => JSON.stringify({ error: err.message }));
  `
});

// Step 4: 输出结果
text('抖音搜索: ' + dyResult.content[0].text);
text('小红书搜索: ' + xhsResult.content[0].text);

// Step 5: 分析并生成选题报告（由 LLM 分析数据，无需 API 调用）
```

> **提示**：以上示例展示了完整的工作流。实际使用时，Step 2 和 Step 3 可以并行执行（使用 Promise.all），Step 5 的分析报告由 Skill 根据返回数据自动生成。
